# Customer Segmentation App 📊

This is a Machine Learning Web App to segment customers based on their buying behavior using **RFM Analysis** and **K-Means Clustering**.

## How to Run
1. Install dependencies:

   pip install -r requirements.txt
Run the app:

streamlit run app.py